const tree = (jsx("fragment", {
    children: ["Be aware,", jsx("jsx", {}), "elements may appear", jsx("in", {
            "any": jsx("place", {})
        }), ". You", jsx("must", {
            "handle": them in a ? jsx("recursive", {}) : jsx(Way, {})
        }), "Look at", jsx("i-this", {
            children: a ? { x: jsx("a", {}) } : null
        })]
}));
export {};
