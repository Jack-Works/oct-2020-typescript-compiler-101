const bool = <a open />
const str = <a href="nyaa" />
const expr = <a id={window} />
const spread = <a {...window} />
const all = (
    <nyaa
        closing
        href="somewhere"
        id={window}
        {...window}
        order={'should correct'}
        {...navigator}
        data-one-more-attribute
    />
)
export {}
