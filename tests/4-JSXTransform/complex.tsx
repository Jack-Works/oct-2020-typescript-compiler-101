export {}
declare const them: string
declare const a: any
declare const Way: any

const tree = (
    <>
        Be aware, <jsx /> elements may appear
        <in any={<place />} />. You <must handle={them in a ? <recursive /> : <Way />} />
        Look at <i-this>{a ? { x: <a></a> } : null}</i-this>
    </>
)
